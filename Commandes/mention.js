const Discord = require('discord.js');
const client = new Discord.Client();
module.exports.run = (client, message, args) => {
        let membre = message.mentions.members

        message.channel.send(user.toString() + ' message text');
}
      module.exports.help = {
        name: 'mention'
    };


    